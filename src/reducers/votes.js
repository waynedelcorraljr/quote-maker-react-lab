// export default (state = [], action) => {

//     switch(action.type) {
  
//     //   case 'UPVOTE_QUOTE':
  
//     //     return state.map(quote => quote.id !== action.quoteId ? quote : {id: quote.id, content: quote.content, author: quote.author, votes: (quote.votes + 1)}) 
  
//       case 'DOWNVOTE_QUOTE':

//         console.log(state)
  
        // return state.map(quote => {
        //     if (quote.id === action.quoteId) {
        //         const downQuote = {
        //             id: quote.id,
        //             content: quote.content,
        //             author: quote.author,
        //             votes: quote.votes - 1
        //         }
        //         return downQuote
        //     } else {
        //         return quote
        //     }  
        // })
  
//       default: 
//         return state;
//     }
  
//   }